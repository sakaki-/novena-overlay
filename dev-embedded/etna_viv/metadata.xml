<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>sakaki@deciban.com</email>
		<name>sakaki</name>
	</maintainer>
	<upstream>
		<remote-id type="github">etnaviv/etna_viv</remote-id>
	</upstream>
	<longdescription>
		FOSS user-space driver for the Vivante GCxxx series embedded GPUs.

		This package provides libetnaviv (and headers); a library for:
			a) ioctl (kernel interface) wrapping;
			b) video memory management;
			c) command buffer and event queue handling;
			d) context / state delta handling (still incomplete);
			e) register description headers; and
			f) converting surfaces and textures from and to Vivante
			   specific tiling formats.

		Currently used only by the 3D driver in driver/.
		A future 2D, SVG or OpenCL driver can share this code.

		This library completely wraps the kernel interface, so that clients don't
		depend on the specific headers.
	</longdescription>
</pkgmetadata>